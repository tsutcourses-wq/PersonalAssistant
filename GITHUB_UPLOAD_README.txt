ANDROID v3.8.1 - GITHUB WORKFLOW NOTE

The required GitHub Actions workflow is included at:
  .github/workflows/build-apk.yml

A second visible copy is included at:
  GITHUB_WORKFLOW_TEMPLATE/build-apk.yml

If .github is missing after extraction or upload, double-click:
  CREATE_GITHUB_FOLDER.bat

Alternatively, on GitHub choose Add file > Create new file and enter:
  .github/workflows/build-apk.yml
Then paste the contents of GITHUB_WORKFLOW_TEMPLATE/build-apk.yml.

The ZIP itself has .github at the archive ROOT (not inside another parent folder).
